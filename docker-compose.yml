services:
  crossdock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crossdock-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
